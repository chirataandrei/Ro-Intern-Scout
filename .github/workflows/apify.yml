name: Apify refresh (discovery + Wellfound)

on:
  schedule:
    - cron: "30 3,11,17 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: apify
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Refresh Apify cache
        env:
          APIFY_TOKEN: ${{ secrets.APIFY_TOKEN }}
        run: python -m internscout apify-refresh ${{ github.event_name == 'workflow_dispatch' && '--force' || '' }}

      - name: Persist Apify artifacts
        run: |
          git config user.name "internscout-bot"
          git config user.email "internscout-bot@users.noreply.github.com"
          git add data/apify_cache.json data/apify_state.json data/discovered.json
          if git diff --staged --quiet; then
            echo "No Apify artifact changes"
          else
            git commit -m "chore: refresh Apify cache and discovered boards"
            git pull --rebase origin main
            git push
          fi
